<?php

namespace App\Providers;

use Admin\Events\AdminUserApplicationConfirmed;
use Admin\Listeners\AdminProcessUserApplication;
use Illuminate\Auth\Events\Registered;
use Illuminate\Auth\Listeners\SendEmailVerificationNotification;
use Illuminate\Foundation\Support\Providers\EventServiceProvider as ServiceProvider;
use Lk\Events\UserApplicationCreated;
use Lk\Listeners\ProcessUserApplication;

class EventServiceProvider extends ServiceProvider
{
    /**
     * The event to listener mappings for the application.
     *
     * @var array<class-string, array<int, class-string>>
     */
    protected $listen = [
        Registered::class => [
            SendEmailVerificationNotification::class,
        ],
        UserApplicationCreated::class => [
            ProcessUserApplication::class,
        ],
        AdminUserApplicationConfirmed::class => [
            AdminProcessUserApplication::class,
        ],
    ];

    /**
     * Register any events for your application.
     *
     * @return void
     */
    public function boot()
    {
        //
    }

    /**
     * Determine if events and listeners should be automatically discovered.
     *
     * @return bool
     */
    public function shouldDiscoverEvents()
    {
        return false;
    }
}
